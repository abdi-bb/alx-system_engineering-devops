#!/usr/bin/env bash
# Displays "Best School" 10 times but for the 9th iteration, displays "Hi" additionally

count=0
while [ $count -lt 10 ]
do
	echo "Best School"
	if [ $count -eq 8 ]
	then
		echo "Hi"
	fi
	count=$((count+1))
done
